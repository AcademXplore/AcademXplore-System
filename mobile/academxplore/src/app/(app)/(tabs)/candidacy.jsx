import { Tabs } from "expo-router";
import { Text, View } from "react-native";

export default function Candidacy(){
  
  return(
    <View style={{flex: 1, backgroundColor: 'gray'}}>
      
      <Text style={{color: "#fff"}}>Candidacy</Text>
    </View>
  )
}